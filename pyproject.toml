[project]
name = "invoice-processor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dateparser>=1.3.0",
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
]
